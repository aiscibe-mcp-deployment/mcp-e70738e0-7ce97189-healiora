{
  "name": "healiora",
  "version": "1.0.0",
  "description": "MCP Server for healiora",
  "scripts": {
    "start": "python app/mcp_server.py",
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {},
  "engines": {
    "node": "24.x"
  }
}